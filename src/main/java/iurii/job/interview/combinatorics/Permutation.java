package iurii.job.interview.combinatorics;

/**
 * P(n) = n! - standard permutation of n elements
 * P(k from n) = k ^ n - permutation of set k elements with repetitions of elements. Ex. n letter word from alphabet k
 * P(m1, m2 .. n) = n! / (m1! * m2!) - permutation of multi sets. elements of each set are considered equal
 */
public class Permutation {
}
