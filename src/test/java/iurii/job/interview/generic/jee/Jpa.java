package iurii.job.interview.generic.jee;

/**
 * @JoinColumn (foreign key) is needed for configuration OneToMany in order to have two tables instead of three (one to join two ids)
 *
 */
public interface Jpa {
}
